import { VanillaTiltDirective } from './vanilla-tilt.directive';

describe('VanillaTiltDirective', () => {
  it('should create an instance', () => {
    const directive = new VanillaTiltDirective();
    expect(directive).toBeTruthy();
  });
});
